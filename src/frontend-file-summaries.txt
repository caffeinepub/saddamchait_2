{
  "frontend/src/App.tsx": "Main app component managing navigation state between Login, Signup, and PasswordReset screens using local UI-only state (no routing library).",
  "frontend/src/screens/LoginScreen.tsx": "Login screen with Firebase Email/Password authentication and Firestore approval gating that blocks unapproved or missing-profile users with the exact message \"Your account is pending admin approval.\" and displays user-friendly Firebase error messages, with comprehensive console logging for debugging approval flow.",
  "frontend/src/screens/SignupScreen.tsx": "Signup screen with profile photo capture, form validation, Firebase Auth user creation, Firestore profile storage with approved=false and role assignment (super_admin for first user, user otherwise), immediate sign-out after signup, distinct error handling for profile creation failures, and success screen display only when both Auth and Firestore operations succeed.",
  "frontend/src/components/auth/AuthLayout.tsx": "Reusable auth layout wrapper providing consistent mobile-first centered layout, spacing, and footer with attribution for both auth screens.",
  "frontend/src/main.tsx": "Application entry point that renders App wrapped in QueryClientProvider only (Internet Identity provider removed for UI-only phase).",
  "frontend/src/index.css": "Global styles with Tailwind imports and custom OKLCH color system using a warm teal/green primary palette (avoiding blue/purple) with proper light/dark mode support.",
  "frontend/tailwind.config.js": "Tailwind configuration with extended color tokens mapped to CSS variables and consistent design system settings.",
  "frontend/src/lib/profileImageProcessing.ts": "Client-side utility for processing profile images - resizes to max 512x512, compresses to JPEG at 70% quality, validates size under 200KB, and returns processed file with metadata.",
  "frontend/src/screens/PasswordResetScreen.tsx": "UI-only password reset screen with explicit Admin-only manual reset messaging in both pre-submit instructions and post-submit confirmation, using Info icon and Alert components, with no Firebase password reset API calls.",
  "frontend/src/lib/firebase.ts": "Firebase wrapper module that uses getDocFromServer() to force fresh server reads bypassing cache entirely on every login, ensuring approval state is never cached and always reflects current Firestore data at users/{auth.uid}.",
  "frontend/src/lib/firebaseTypes.ts": "TypeScript interfaces for Firebase user profile data structure with role union including 'super_admin', 'admin', and 'user', and signup data payload.",
  "frontend/src/lib/firebaseErrorMessages.ts": "Utility function that maps Firebase Auth, Firestore, and custom profile creation error codes to user-friendly English error messages.",
  "frontend/index.html": "HTML entry point with Firebase SDK loaded via CDN (v10.8.0) and initialized with project configuration, exposing Firebase app, auth, and firestore instances on window object for use by the application.",
  "frontend/DEPLOYMENT.md": "Comprehensive deployment runbook documenting steps to promote draft to LIVE including Firebase setup, build commands, deployment verification checklist with critical approval gating checks ensuring fresh server reads from users/{auth.uid} using getDocFromServer() with fromCache:false indicator, no caching, rollback procedures, troubleshooting guidelines for approval-related issues, and expected console log patterns.",
  "frontend/LIVE_SMOKE_TEST.md": "Detailed step-by-step smoke test checklist covering signup flow, first-user super_admin auto-approval, critical approval gating tests verifying fresh server reads using getDocFromServer() with fromCache:false indicator, document path verification ensuring users/{auth.uid} is used, security rules validation, and missing profile handling with explicit boolean checking and expected console log patterns.",
  "frontend/firestore.rules": "Firestore security rules enforcing role-based access control where only admins can modify role and approved fields, regular users can update their own profiles except for role/approved fields, and all users can read their own data while admins can read all user data."
}